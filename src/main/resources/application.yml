server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  application:
    name: attendance-system
  profiles:
    active: dev # Si no se especifica perfil, arranca en modo desarrollo por defecto.
#  jpa:
#    hibernate:
#      ddl-auto: validate
#    show-sql: true




#logging:
#  level:
#    root: DEBUG
#    org.springframework.web: DEBUG
#    com.school.attendance_system: DEBUG
#    org.hibernate.SQL: DEBUG
#    org.hibernate.type.descriptor.sql: TRACE
